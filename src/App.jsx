import React from "react";
import Api from "./Components/Api";

function App() {

  return(

  
   <Api/>
  );
}

export default App;


   // const filteredItems = data.filter(item => item.title === "Title");
        // console.log(filteredItems)

        // const [filter] = useState(filteredItems);

    // Create the following components:
    // MovieCard
    // MovieList
    // Filter ( title, rate)
    // Every movie should have the following attributes: title, description, posterURL, rating
    // The user should be:
    // Able to add a new movie.
    // Filter the movies with title/rating.



    //if there is no data , leave it in loading
